# Server
PORT=3000
NODE_ENV=development

# Database (valores padrão compatíveis com docker-compose)
DB_HOST=localhost
DB_PORT=5432
DB_USER=timecontrol
DB_PASSWORD=timecontrol_secret
DB_NAME=timecontrol

# CORS (URL do frontend Angular)
FRONTEND_URL=http://localhost:4200
